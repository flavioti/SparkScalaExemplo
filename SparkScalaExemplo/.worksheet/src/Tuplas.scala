object Tuplas {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(35); 
  println("TUPLAS");$skip(28); 
	val tupla = "Flavio" -> 34;System.out.println("""tupla  : (String, Int) = """ + $show(tupla ))}
}
